<Application x:Class="ShoeStoreDesktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ShoeStoreDesktop"
             xmlns:converters="clr-namespace:ShoeStoreDesktop.Converters"
             Startup="Application_Startup">
    <Application.Resources>
        <converters:StringToImageConverter x:Key="StringToImageConverter"/>
        <converters:QuantityToTextConverter x:Key="QuantityToTextConverter"/>
        <converters:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
        <converters:DiscountToBackgroundConverter x:Key="DiscountToBackgroundConverter"/>
        <converters:DiscountToForegroundConverter x:Key="DiscountToForegroundConverter"/>
        <converters:QuantityToBackgroundConverter x:Key="QuantityToBackgroundConverter"/>
        <converters:PriceDisplayConverter x:Key="PriceDisplayConverter"/>
        <converters:DiscountToTextConverter x:Key="DiscountToTextConverter"/>
        <converters:DiscountLabelConverter x:Key="DiscountLabelConverter"/>
        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Icon" Value="Icon.ico"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#7FFF00"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#000000"/>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#00FA9A"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="BorderBrush" Value="#00CD8B"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="SecondaryPanel" TargetType="Border">
            <Setter Property="Background" Value="#F8FFF0"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#7FFF00"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="SecondaryHeader" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#7FFF00"/>
            <Setter Property="BorderThickness" Value="0,0,0,3"/>
        </Style>

        <Style x:Key="DiscountHighStyle" TargetType="Border">
            <Setter Property="Background" Value="#2E8B57"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="5,2"/>
        </Style>

        <Style x:Key="OutOfStockStyle" TargetType="Border">
            <Setter Property="Background" Value="#E8F4FF"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="5,2"/>
        </Style>

        <Style x:Key="OldPriceStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#999999"/>
            <Setter Property="TextDecorations" Value="Strikethrough"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>

        <Style x:Key="NewPriceStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,2,0,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="BorderBrush" Value="#7FFF00"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,2,0,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="BorderBrush" Value="#7FFF00"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,2,0,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="BorderBrush" Value="#7FFF00"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="Margin" Value="0,10,0,2"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>

        <Style x:Key="FormGroup" TargetType="StackPanel">
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="WindowTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Foreground" Value="#2E8B57"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="BorderBrush" Value="#7FFF00"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F0F0F0"/>
            <Setter Property="VerticalGridLinesBrush" Value="#F0F0F0"/>
            <Setter Property="RowBackground" Value="#FFFFFF"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FFF0"/>
        </Style>

        <Style TargetType="StatusBar">
            <Setter Property="Background" Value="#F8FFF0"/>
            <Setter Property="BorderBrush" Value="#7FFF00"/>
            <Setter Property="BorderThickness" Value="1,0,0,0"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>
    </Application.Resources>
</Application>