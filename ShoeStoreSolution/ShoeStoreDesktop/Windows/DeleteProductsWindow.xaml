<Window x:Class="ShoeStoreDesktop.Windows.DeleteProductsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:ShoeStoreDesktop.Converters"
        Title="Подтверждение удаления товаров" 
        Height="600" 
        Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Icon="/Icon.ico">

    <Window.Resources>
        <converters:QuantityToTextConverter x:Key="QuantityToTextConverter"/>
    </Window.Resources>

    <Grid Background="#FFFFFF">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#FFF0F0" 
                BorderBrush="#FF9999" 
                BorderThickness="0,0,0,1"
                Padding="15">
            <StackPanel>
                <TextBlock Text="УДАЛЕНИЕ ТОВАРОВ" 
                           FontSize="18" 
                           FontWeight="Bold"
                           Foreground="#CC0000"/>
                <TextBlock Text="Будут удалены выбранные товары. Эта операция необратима." 
                           Margin="0,5,0,0"
                           FontSize="14"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" 
                       Text="Список товаров для удаления:" 
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>

            <DataGrid x:Name="ProductsDataGrid" 
                      Grid.Row="1"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserResizeRows="False"
                      SelectionMode="Single"
                      BorderBrush="#FF9999"
                      BorderThickness="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Артикул" 
                                        Binding="{Binding Article}" 
                                        Width="80"/>
                    <DataGridTextColumn Header="Наименование" 
                                        Binding="{Binding Name}" 
                                        Width="*"/>
                    <DataGridTextColumn Header="Производитель" 
                                        Binding="{Binding Manufacturer.Name}" 
                                        Width="150"/>
                    <DataGridTextColumn Header="Цена" 
                                        Binding="{Binding Price, StringFormat={}{0:N2} ₽}" 
                                        Width="80"/>
                    <DataGridTextColumn Header="Скидка" 
                                        Binding="{Binding Discount, StringFormat={}{0}%}" 
                                        Width="70"/>
                    <DataGridTextColumn Header="Наличие" 
                                        Width="80">
                        <DataGridTextColumn.Binding>
                            <Binding Path="Quantity">
                                <Binding.Converter>
                                    <converters:QuantityToTextConverter/>
                                </Binding.Converter>
                            </Binding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Border Grid.Row="2" 
                Background="#F8F8F8" 
                BorderBrush="#DDDDDD" 
                BorderThickness="0,1,0,0"
                Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock x:Name="SelectedCountTextBlock" 
                           Text="Выбрано: 0 товаров"
                           VerticalAlignment="Center"
                           Margin="0,0,20,0"
                           FontWeight="Bold"/>

                <Button x:Name="CancelButton" 
                        Content="Отмена" 
                        Width="120"
                        Click="CancelButton_Click"/>

                <Button x:Name="DeleteButton" 
                        Content="Удалить выбранные" 
                        Background="#CC0000"
                        Foreground="White"
                        BorderBrush="#990000"
                        FontWeight="Bold"
                        Width="150"
                        Margin="10,0,0,0"
                        Click="DeleteButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>